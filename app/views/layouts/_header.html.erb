<nav class="navbar navbar-static-top navbar-default" role="navigation">
<div class="container">
  <!-- Brand and toggle get grouped for better mobile display -->
  <div class="navbar-header">
    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    <%= link_to image_tag('logo4.png', :class => 'logo_style'), root_path, class: "navbar-brand" %>
  </div>

  <!-- Collect the nav links, forms, and other content for toggling -->
  <div class="collapse navbar-collapse navbar-ex1-collapse">
    <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li>
            <%= link_to new_pin_path do %>
              <span class="glyphicon glyphicon-plus"></span> New Pin
            <% end %>
          </li>
          <% for user in User.all %>
          <li class="dropdown">
            <div id="navbar-text">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= image_tag user.avatar.url(:mini), class: "img-rounded" %></a>
            </div>
            <ul class="dropdown-menu" role="menu">
            <li>
              <%= link_to current_user do %>
                <span class="glyphicon glyphicon-user"></span>
                 <%= current_user.first_name %> <%= current_user.last_name %>
              <% end %>
            </li> 
            <li>
              <%= link_to edit_user_registration_path do %>
              <span class="glyphicon glyphicon-cog"></span>
                Edit account
              <% end %>
            </li> 
            <li class="divider"></li>
            <li>
              <%= link_to destroy_user_session_path, method: :delete do %>
                <span class="glyphicon glyphicon-log-out"></span> Log out
              <% end %>
            </li>
            </ul>
          </li>
          <% end %>
        <% else %>
          <li>
            <div id='navbar-login'>
              <%= render 'layouts/login' %>
            </div>
          </li>
        <% end %>
    </ul>
    </div><!-- /.container -->
  </div><!-- /.navbar-collapse -->
</nav>